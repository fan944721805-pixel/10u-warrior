# 下注方向复盘与整体修正

本次只修改方向判断。加码、加仓、情绪、金额阶梯、梭哈条件和本金均保留。修改已在代码及隔离模拟中验证，当前运行服务未重启；下次启动加载新版。历史订单、余额和结算记录没有改写。

## 历史下注

统计快照：2026-09-13 14:53:08（北京时间）。数据来自本机模拟账本，不代表真实成交。未结算订单不计入已实现盈亏，平局不计入方向胜率。

| 范围 | 已结算 | 胜 / 负 / 平 | 方向胜率 | 已实现盈亏 |
| --- | ---: | ---: | ---: | ---: |
| 当前第一、二、三局 | 192 | 108 / 76 / 8 | 58.70% | -247.02U |
| 含旧局、归档去重后的全部记录 | 669 | 288 / 347 / 34 | 45.35% | -946.44U |

当前还有 17 笔未结算。全部历史混合多个代码版本及开发期间对局，部分旧单缺少完整来源标记，因此优先用当前三局定位问题，不能把旧局汇总当作同一策略的长期实盘表现。账本均未标记已包含手续费，表中盈亏未作手续费推算。

| 当前策略 | 胜 / 负 / 平 | 方向胜率 | 已实现盈亏 |
| --- | ---: | ---: | ---: |
| 赌狗 aggressive | 24 / 13 / 1 | 64.86% | -96.78U |
| 超级 AI smart | 30 / 12 / 1 | 71.43% | +106.18U |
| 追涨杀跌 trendFollowing | 11 / 7 / 1 | 61.11% | -32.44U |
| 抄底摸顶 meanReversion | 10 / 16 / 1 | 38.46% | -103.48U |
| 裸 K priceAction | 14 / 11 / 1 | 56.00% | -77.49U |
| 突破 breakout | 4 / 3 / 0 | 57.14% | -4.47U |
| 风水 fengShui | 10 / 7 / 2 | 58.82% | -7.33U |
| 占卜 diviner | 5 / 7 / 1 | 41.67% | -31.21U |

策略样本很小，且多个 Agent 共用同一市场结果，不能当作相互独立的数百次实验。上表使用稳定策略键辅助识别，用户自定义名称不参与分类。

## 亏损原因与证据

| 证据 | 判断 | 已处理或后续边界 |
| --- | --- | --- |
| 当前开盘进场 118 笔结算，74 胜、40 负、4 平，+633.02U；signal 模式 74 笔结算，34 胜、36 负、4 平，-880.04U | 后一时段表现明显恶化，但仅覆盖 15 个市场，而且 signal 模式也可能在开盘几秒内下单，不能归因于“盘中机制本身有害” | 保留实时进场及其轮询、冷却，不关闭功能换取漂亮数据 |
| 原 `buildDecisionContext` 传入短期涨跌、指标和赔率，却没有本轮开盘位置；`strategySignal` 不使用已发生位移和剩余时间 | 预测下一段涨跌与预测本轮最终结算方向混淆，反弹信号可能仍应结算 DOWN | 增加所有技术策略共用的结算方向校正层 |
| 当前 meanReversion 仅 38.46% 胜率；priceAction 56% 胜率仍亏损 | 抄底信号不等于五分钟内能涨回起点；形态同向也不代表有足够赔率 | 用当时开盘距离、剩余波动空间和两侧报价交叉检查，不提高出手门槛 |
| 当前平均模型 confidence 为 79.47%，实际二元胜率只有 58.70% | confidence 是评分曲线输出，不能当作已验证概率。这两个总体口径也不构成正式分桶校准 | 本轮不调高 confidence 来强行让低赔率过关，不拟合这几十个市场来宣称校准成功 |
| 当前赌狗按每单固定 1U 换算收益合计约 +2.36U，实际仓位收益为负；全部策略固定 1U 换算合计也为负 | 方向、成交赔率和仓位路径共同决定盈亏。胜率超过 50% 本身不足以保证赚钱 | 按用户要求，仓位路径保持原样；只报告方向修正带来的独立变化 |

例子：某轮先从 100 跌至 99，再反弹至 99.2。裸 K 可以给出“反弹向上”，但如果剩余时间内难以回到 100 以上，结算方向仍可能是 DOWN。原逻辑缺少这个共同基准。

## 本次整体修正

`round-direction.js` 从决策当时已取得的数据构建上下文：优先采用市场已提供的开盘价；没有时，只使用精确对应本轮开始时间的现货 K 线开盘价，并明确标记 `spot-open-proxy`。当前价同样明确标记为现货代理，不冒充官方 Chainlink 结算价。不使用结算后的开盘补录、终盘价或未来 K 线。

当本轮已经过去至少 20%、开盘位移超过剩余时间的一倍历史波动尺度，且两侧报价支持该方向时，如果原技术信号仅指向相反的短线反弹/回落，则把方向对齐到结算目标。历史波动来自当时已经收盘且连续的 1 分钟价格；波动距离只是启发式尺度，不是统计置信区间。

这层适用于 aggressive、smart、conservative、trendFollowing、meanReversion、breakout、orderFlow、volatilityGuard、consensus、priceAction 共 10 种技术策略。保留原始信号强度，不修改情绪和下注金额公式，不把原来没有信号的 SKIP 强行变成下注。固定单边角色、针对其他 Agent 的反向角色和冻结占卜结果保留其明确规则；不会为了统一方向破坏这些角色的设定。纯离线随机行情演示缺少真实本轮基准，仍按其原规则运行。

校正同时用于进场信号、mock 决策、外部模型提示和最终方向校验。订单审计记录保存 `directionVersion`、`roundContext` 和发生变化时的 `directionCorrection`，可以看到从哪个方向改到哪个方向、依据是什么。

## 回放结果与下注频率

冻结的 467 条有已知结算结果的决策输入覆盖 59 个资产市场轮次。前后使用相同输入、账户余额、连胜连败、仓位公式和当时报价，分别限制每 Agent 每轮最多成交一次。

| 同输入对照 | 修改前 | 修改后 |
| --- | ---: | ---: |
| 可模拟下注次数 | 236 | 234 |
| 胜 / 负 / 平 | 129 / 99 / 8 | 130 / 96 / 8 |
| 固定历史账户输入下逐单收益合计 | +69.90U | +113.91U |
| 每单固定 1U 收益合计 | -4.80U | -1.52U |

出手保留率 **99.15%**。实际只改变了三次决策：BTC 抄底单由错误 UP 修正为正确 DOWN，金额仍为 9.28U；另两次反向校正后正确一侧赔率过低，原有正期望门禁选择 SKIP。约 44.01U 的对照改善中，约 11.90U 来自方向改正，约 32.11U 来自这两次跳过，不能全部归功于方向命中率提升。

这是诊断样本内的冻结输入对照，不是资金复利回测，也不是未来验证。旧版本配置、真实执行时延、fresh book 与快照 book 差异使回放总下注数和真实历史不同；未成交轮次的结算缺失也限制覆盖面。反向成交重新按当时对侧订单簿估算，未采用未来报价、未计手续费。禁止将这里的 +113.91U 与历史实际 -247.02U 当成可直接替换的资金曲线。

后续是否继续扩大方向修正，应看新的结算记录及独立时间段，而不能在同一批亏损订单上不断调参数。本次没有自动翻转所有亏损策略、降低下注频率设置、修改 confidence 曲线或做历史结果泄漏的拟合。

## 验证与复现

`npm run check` 通过。全量原有及新增测试通过 212 项，覆盖对称方向修正、缺失/过期基准、未来 K 线排除、金额与风险模式不变、旧方向拒绝、模拟下单及审计。隔离浏览器验证中文、英文、日文、韩文，以及 360 / 768 / 1440px 宽度；没有钱包、外部 AI 或真实交易操作。

本次修改文件的空白检查通过。仓库全局 `git diff --check` 仍报告本次未修改的 `public/agent-setup.js` 和 `public/app.js` 末尾空行，保留用户已有改动。

在项目目录执行：

```powershell
node scripts/audit-betting.cjs
node scripts/replay-direction.cjs .data/direction-review-input.json
node --test test/round-direction.test.js
```

重新采集未来记录时使用另一个文件名，保留本轮冻结输入：

```powershell
node scripts/replay-direction.cjs --capture .data .data/direction-review-next.json
node scripts/replay-direction.cjs .data/direction-review-next.json
```

本轮统计原件：`.data/betting-audit-snapshot.json`；回放结果：`.data/direction-replay-result.json`。这些文件只用于复盘，不参与运行策略或改写账本。
